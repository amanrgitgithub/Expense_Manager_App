import 'package:flutter/material.dart';

class MonthlyExpensePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Display monthly expenses here'),
      // Use a ListView or another widget to show monthly expenses
    );
  }
}
