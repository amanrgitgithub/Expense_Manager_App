import 'package:flutter/material.dart';

class WeeklyExpensePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Display weekly expenses here'),
      // Use a ListView or another widget to show weekly expenses
    );
  }
}
